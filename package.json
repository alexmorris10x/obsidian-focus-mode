{
  "name": "obsidian-focus-mode",
  "version": "0.3.1",
  "description": "Obsidian plugin that defaults to a distraction-free view and toggles chrome on document taps.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "check": "tsc --noEmit",
    "dev": "node esbuild.mjs --watch"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "focus-mode",
    "fullscreen"
  ],
  "author": "10x",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "obsidian": "^1.12.3",
    "typescript": "^6.0.2"
  }
}
